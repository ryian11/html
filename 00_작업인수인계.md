# CJ 중3 영어(소영순) 전자저작물 — Dictation 팝업 작업 인수인계

이 문서 하나만 읽고 새 세션에서 바로 이어서 작업할 수 있게 정리했다.
**작업 대상**: 스토리보드·사운드 대본 엑셀 2종 → `popup/pNNN_NN_dic1.html` (딕테이션 팝업)

---

## 0. 지금까지 끝난 것

| 단원 | 만든 파일 | 상태 |
|---|---|---|
| 6 | `lesson06/ops/popup/` p104_02 · p105_01 · p106_01 · p107_01 | Reading 4개만 신규 (앞부분 3개는 기존 파일 그대로) |
| 7 | `lesson07/ops/popup/` p118_03 · p119_01 · p120_02 · p122_02 · p123_01 · p124_01 · p125_01 | 7개 전부 |
| 8 | `lesson08/ops/popup/` p136_03 · p137_01 · p138_02 · p140_02 · p141_01 · p142_01 · p143_01 | 7개 전부 |

3단원(lesson03)은 **원본 참고용**. 절대 수정하지 말 것.

---

## 1. 입력 파일 (사용자 PC)

```
스토리보드  E:\00_works\2026\2026_cj_midd3_eng\01_스토리보드\전자저작물 추가 원고_20260716_아이스캔디 전달\
            3학년 전자저작물_지시문_딕테이션_미니 단어장_구문 해설_Lesson N.xlsx
사운드대본  E:\00_works\2026\2026_cj_midd3_eng\02_사운드\
            중3(소영순) 전자저작물 녹음 대본_최종_20260806 아이스캔디 전달.xlsx
산출물      E:\00_works\2026\2026_cj_midd3_eng\00_개발물\EBOOK\중학교 영어 3_소영순\
            app\resource\contents\lessonNN\ops\popup\
생성기      E:\00_works\2026\2026_cj_midd3_eng\00_개발물\_딕테이션_생성기\   (이 문서와 스크립트)
```

연결된 폴더(device_stage_files / device_commit_files 로 접근):
`E:\00_works\2026\2026_cj_midd3_eng` 와 `...\app\resource\contents` 두 개면 충분하다.
`device_bash` 는 Windows 업데이트(2026-09-08) 이후 마운트가 깨져 있으니 쓰지 말 것.

---

## 2. 새 단원 작업 절차

### 준비 (한 번만)

```bash
# 1) 생성기 스크립트를 컨테이너로 복사 (device_stage_files 로 _딕테이션_생성기 폴더 스테이징 후)
mkdir -p /home/claude/cj/measure && cp <스테이징경로>/*.py /home/claude/cj/ \
  && cp <스테이징경로>/build3.py <스테이징경로>/opt.py <스테이징경로>/geo.js /home/claude/cj/measure/

# 2) 렌더링에 필요한 lesson03 폴더 스테이징 (include 의 css·font·js 전부 + popup 참고 원본)
#    → /mnt/user-data/uploads/resource--contents/lesson03/ops/{include,popup}
#    ※ 심볼릭 링크 안 됨. /include/js/handlebars.js 를 루트 절대경로로 부르는 코드가 있어서
#      반드시 lesson03/ops/popup 안에서 렌더링해야 한다.

# 3) 해당 단원 스토리보드 xlsx 스테이징
#    → /mnt/user-data/uploads/2026_cj_midd3_eng/01_스토리보드/.../Lesson N.xlsx
```

### 1단계 — 단원 구조 파악

```bash
python3 -c "
import openpyxl,re,src
ws=openpyxl.load_workbook(src.SND,data_only=True)['N과']
seen={}
for r in ws.iter_rows(values_only=True):
    fn=str(r[2] or '').strip()
    m=re.match(r'^3-(\d+)-(LnT-A|LnT-B-1|EtC-A|Read)-',fn)
    if m: seen.setdefault(m.group(0)[:-1],0); seen[m.group(0)[:-1]]+=1
print(seen)"
```
→ 각 코너의 **쪽 번호**와 mp3 접두사가 나온다. (예: `3-118-LnT-A`, `3-122-Read`)

그다음 딕테이션 원고를 눈으로 본다.
```bash
python3 -c "
import openpyxl,src,textwrap
ws=openpyxl.load_workbook(src.SB%N,data_only=True,rich_text=True)['Dictation']
for row in range(2,ws.max_row+1):
    sec=str(ws.cell(row=row,column=1).value or '').strip()
    lv=str(ws.cell(row=row,column=2).value or '').strip()
    if lv!='기본' or not sec: continue
    v=ws.cell(row=row,column=3).value
    t=''.join(b if isinstance(b,str) else b.text for b in ([v] if isinstance(v,str) else v))
    print('#####',sec)
    for i,l in enumerate(t.split('\n')):
        print(' %2d [들여쓰기%d] %s'%(i,len(l)-len(l.lstrip()),l.strip()[:80]))"
```

**여기서 판단할 것 — 사용자에게 물어볼 것**
- 파일명 뒤 번호(`_01`·`_02`·`_03`)는 쪽 안의 블록 순서라 단원마다 다르다.
  3단원 기준 offset 으로 추정하되(L6 +54 / L7 +72 / L8 +90) **반드시 확인**.
  (6단원은 LnT-A·B 가 둘 다 p100 에 있어 앞부분 규칙이 아예 다르다)
- Reading 이 인터뷰(화자 있음)인지 산문인지 → 아래 3장 참고

### 2단계 — 생성기 선택

| 원고 모양 | 생성기 | 예 |
|---|---|---|
| 화자 있는 대화 (`G:` `M:` / `Emma:` / `Q.`) | `dlg.py` | LnT-A·B, EtC, L7 Reading |
| 화자 없는 산문 (+ 소제목 가능) | `rd.py` | L6·L8 Reading |

`dlg.py` / `rd.py` 맨 아래 `LESSONS` 딕셔너리에 단원을 추가한다.

```python
# dlg.py
LESSONS = {
 9: [dict(page='p1NN_0N', sec='Listen and Talk A', pre='3-1NN-LnT-A'),
     dict(page='p1NN_0N', sec='Listen and Talk B', pre='3-1NN-LnT-B-1'),
     dict(page='p1NN_0N', sec='Enjoy the Clip',    pre='3-1NN-EtC-A'),
     # Reading 이 인터뷰면 아래처럼 (4개가 한 인터뷰 → 화자칸 폭 공유)
     dict(page='p1NN_0N', sec='Reading 1', pre='3-1NN-Read', dialog=True, spkgroup='L9read'),
    ],
}
# rd.py
LESSONS = {9: [('p1NN_0N','Reading 1','1NN'), ...]}
```

### 3단계 — 본문 생성

```bash
cd /home/claude/cj
rm -f measure/work/popup/*.html
python3 dlg.py 9        # 또는 python3 rd.py 9  (둘 다면 차례로, work/popup 에 쌓인다)
```
경고가 뜨면 반드시 확인한다.
- `[경고] 매칭 실패: …` → 원고와 음원이 안 맞음. 내용 확인 필요.
- `[건너뜀] 원고에 없는 음원: …` → 제목·라벨·번호 안내면 정상.

### 4단계 — 입력칸 폭 · scrollTop 주입

```bash
cd /home/claude/cj/measure
rm -f out/*.html
python3 build3.py
```
`work/popup/*.html` 을 읽어 `out/*.html` 로 완성본을 낸다.

### 5단계 — 검증

```bash
cd /home/claude/cj
python3 verify.py measure/out
```
전부 0 이어야 한다. `발화 중간 끊김` 은 독백·긴 문단이면 정상.

본문이 원고와 같은지도 확인한다(대조 스크립트는 7장 참고).

### 6단계 — 납품

```bash
mkdir -p /mnt/user-data/outputs/lessonNN && cp measure/out/*.html /mnt/user-data/outputs/lessonNN/
```
→ `device_commit_files` 로 `...\contents\lessonNN\ops\popup\` 에 커밋.
(`popup` 폴더가 없으면 커밋이 만들어 준다)

---

## 3. 페이지 유형 3가지

| | 대화형 | Reading 산문 | Reading 인터뷰 |
|---|---|---|---|
| 예 | LnT-A·B, EtC | L6·L8 Reading | L7 Reading |
| 생성기 | `dlg.py` | `rd.py` | `dlg.py` (`dialog=True`) |
| 컨테이너 | `data-use="dictation"` | `+ data-type="oneLine"` | `data-use="dictation"` |
| 줄높이 | 1.8 (68.4px) | 2.4 (91.2px) | 1.8 |
| `.Paragraph` | 번호 대화 1개당 1개 | 탭당 1개 | 탭당 1개 |
| `comp-script-box` | 화자 발화 1개 | 원고 문단 1개 | 화자 발화 1개 |
| `speaker` 속성 | `G:` `M:` `B:` `W:` | 없음 | `Q.` `Dr. Roman:` |
| 페이지 style | `.Speaker{width:Npx}` | `.Script:first-of-type{margin-left:20px}` | `.Speaker{width:Npx}` |

**판단 기준**: 화자가 2명 이상 → 대화형(`oneLine` 없음, Speaker 폭 지정).
화자 1명 → `oneLine`, Speaker 폭 미지정. (`dialog=True` 로 강제 가능)

`.Speaker` 폭 = 가장 긴 화자 라벨의 자연 폭(번호 배지 포함) + 8px.
3단원 원본으로 검증: p046_03 자연폭 102→104, p047_01 138→145.
**인터뷰처럼 여러 쪽이 이어지면 `spkgroup` 으로 묶어 폭을 공유**해야 본문 시작 위치가 안 어긋난다.

---

## 4. 원고 읽는 법

### 스토리보드 `Dictation` 시트
7섹션 × 난이도 3(보충/기본/심화) = 21행. A열 섹션, B열 난이도, C열 본문.

- **빨간 글씨(`FFFF0000`) = 빈칸.** `openpyxl.load_workbook(..., rich_text=True)` 로 런 색상을 읽는다.
- **빨강 런 하나 = 정답 버튼 하나.** 런 안의 단어는 각각 별도 빈칸.
  (원고 주석: "단어 하나당 빈칸은 하나씩 / 개별 정답 버튼은 연속된 빈칸은 하나로")
- **문단 나누기**
  - 대화형(`dlg.py`): 원고 한 줄 = 화자 발화 하나. `N. ` 로 시작하면 새 대화(`.Paragraph`).
  - 산문(`rd.py`): 원고 한 줄 = 문단 하나. **소문자로 시작하는 줄만** 앞 문단에 이어붙인다
    (칸 폭 때문에 문장 중간에서 끊긴 경우). 들여쓰기로 판단하면 안 된다 —
    L6 Reading 4 는 소제목·팁이 들여쓰기 0 이다.
- **화자 표기는 원고 것을 쓴다.** 사운드 대본의 `W:`/`B:` 가 아니라 원고의 `Reporter:`/`Daniel:`.

### 사운드 대본 시트 (`N과`)
코너명 / 스크립트 / 파일명 3열.

- 파일명 `3-140-Read-02` → `3_140_read_02.mp3` (대시→언더바, 소문자)
- 스크립트 앞 `W:` `M:` `B:` 는 제거
- **파일명이 빈 행은 앞 음원에 이어붙인다**
- **지문 제목(`Read-00`)은 딕테이션 원고에 없으면 뺀다.** 전체 듣기(`_all`) 팝업 담당이다.
  단 소제목이 원고에 있으면 넣는다(아래).
- **빨간 글씨 파일명 = 추가 녹음분.** 폴더에 없어도 **이름을 바꾸지 말 것**. 나중에 들어온다.
  음원 존재 여부는 확인하지 않는다(사용자 지시).

### 소제목 (`subTitBox`)
Reading 본문에 소제목이 있으면 **자기 `comp-script-box` 에 `class="subTitBox"`**.
페이지 style 의 `:not(.subTitBox)` 규칙 때문에 소제목만 첫 줄 들여쓰기가 빠진다.
3단원 `p050_02_all.html` 이 같은 방식(`titBox`=지문 제목, `subTitBox`=소제목).

**판별**: 음원 하나짜리 문단이고 그 사운드 대본 행이 `M:` 로 시작 → 소제목. (본문은 `W:`)

### 원고 ↔ 대본 정렬
딕테이션 본문을 mp3 문장 순서대로 앞에서부터 잘라 맞춘다(순차 소비).
**글자 완전 일치로 자르면 안 된다** — 둘이 미세하게 다른 경우가 많다.
- 아포스트로피 `'` vs `’`
- 대본 오타 ("How did scientist launch" vs 원고 "scientists")
- 괄호 분리 (원고 "…Telescope (JWST)." 를 대본은 두 음원으로 쪼갬)

`dlg.cut_words()` 가 **음원 문장의 단어 수만큼** 잘라내고 difflib 유사도로 검증한다.
단어 수를 ±2 까지 흔들어 가장 잘 맞는 지점을 고르고, 0.72 미만이면 "원고에 없는 음원"으로
보고 건너뛴다(제목·라벨·번호 안내 `One`/`Two`/`Three`).
여는 괄호·따옴표는 다음 조각으로 넘긴다.
**`‖…‖`(정답 묶음)은 통째로 한 문장에 넣는다 — 중간에서 자르면 안 된다.**

---

## 5. HTML 규칙

- 탭 4개: 1=보충, 2=기본(`selected`), 3=심화, 4=`newBtn`(교사가 직접 만드는 탭)
- 탭 k 의 `data-ply-idx`·`data-scroll-idx`·`handler`·`hdlQuiz` = k-1 (0,1,2 / 탭4는 3)
- `<script-cont>` 속성 순서: `[scrollTop] [class] src en [handler itemIdx]`
- `en` 의 빈칸 표기: `||단어||` (단어마다 하나)
- `itemIdx`: 빈칸마다 0부터 증가. 같은 빨강 런 = 콤마로 묶고, 런 사이는 `|` 로 구분
  예) `itemIdx="9,10|11"` = "this reason"(버튼1) + "for"(버튼2)
- 번호 대화의 첫 box: `number="2" numberSrc="../include/media/common/num2.mp3" numberScrollTop="…"`
  (`numberScrollTop` = 그 box 첫 문장의 `scrollTop`. 0 으로 두면 번호 배지가 안 따라간다)
- 탭4 `sticker` 속성: 문장의 모든 단어를 `|단어|` 로 감싼다
  - 끝의 `.,!?;:` 는 파이프 **밖**: `|bucket|,`
  - 따옴표는 파이프 **안**: `|“Hi!”|`
  - 문장부호만 있는 토큰은 앞 단어에 붙임
- 줄바꿈 **CRLF**, **끝 개행 없음**

---

## 6. 입력칸 폭 · scrollTop (렌더링 실측)

### ★★ 실측 보정 14px — 이거 빼먹으면 전부 어긋난다

**컨테이너의 headless Chromium(Linux)은 실제 기기(Windows)보다 본문 글자를 약 14px 넓게 잡는다.**
그대로 재면 폭이 아슬아슬한 줄이 한 줄 더 접혀서, 그 아래 **모든 scrollTop 이 한 줄(68px)씩** 어긋난다.
재생 중 하이라이트된 문장이 화면 위로 밀려 올라간다.

`build3.CALIB` — 측정할 때만 본문 칸을 14px 넓힌다(산출물에는 안 들어감):
```css
.iframePopup[data-use="dictation"] .tabActBox{ padding-right:1px !important; }   /* 원래 15px */
```
실제 기기 콘솔 값과 대조해 확정했다(p118_03 전체높이 2439→2371, p136_03 2644→2576, 둘 다 실기와 일치).
**증상은 화자 칸이 있는 대화형에서만 난다.** Reading(화자 없음)은 본문이 넓어 보정 전후가 같다.

실기에서 재확인하는 콘솔 한 줄 — **js-scale 배율을 걷어내야 한다**
(`getBoundingClientRect` 는 배율이 곱해지고 `scrollTop` 은 안 곱해진다):
```js
(()=>{const t=document.querySelectorAll('.tabContent')[1],b=t.querySelector('.scrollBox'),
r=b.getBoundingClientRect(),k=r.height/b.clientHeight;
return {전체높이:b.scrollHeight, tops:[...t.querySelectorAll('.scriptTarget')].map(e=>
Math.round((e.getBoundingClientRect().top-r.top)/k+b.scrollTop)).join(' ')}})()
```

### 렌더링 환경
- `--total-width: 1280px` / `--total-height: 720px` → 뷰포트 1280×720 이면 배율 1
- `.scrollBox` 높이 **548px**. 한 줄 = 68.4px(대화형) / 91.2px(oneLine)
- `<comp-script-box>` → `.scriptBox` 변환은 components.js 의 Handlebars 템플릿(순수 치환)
- 문장은 `display:inline` 으로 이어 흐른다 → 짧은 문장 두 개가 한 줄을 공유할 수 있다

### 입력칸 폭 — 정답 글자 실측폭 기준
글자수로 정하면 안 된다(`wondering` 222px vs `patients` 173px, 둘 다 9자).
```
필요폭 = 정답 렌더링 폭(38px gothic_HV) + input 좌우 padding 20px + 오른쪽 여백 14px
input 폭 = 140 / 200 / 250 / 300 / 350 / 400 중 필요폭 이상인 가장 작은 값
```
3단원 원본 201개를 같은 방식으로 재면 잘리는 칸이 하나도 없다(여백 최소 4px, 중앙값 62px).

클래스는 페이지마다 필요한 것만 `<style>` 에 생성한다. `.inputBox` 는 JS 가 만들기 때문에
개별 클래스를 못 붙이고 **부모(script-cont) 클래스 + `nth-of-type`** 으로만 지정할 수 있다.

| 폭 | 전부 | 위치별 |
|---|---|---|
| 140px | `short` | `firstShort` `secondShort` `thirdShort` … `lastShort` |
| 250px | `mid` | `firstMid` … `lastMid` |
| 300px | `wide` | `firstLarge` … `lastLarge` |
| 350px | `toLong` | `firstToLong` … `lastToLong` |

**주의**: 3단원의 `firstLong`/`lastLong`/`.inputBox.long` 은 250px 이라 이름을 분리했다.

### scrollTop
재생 중 그 문장이 화면 위쪽에 오도록 스크롤할 픽셀값.
**문장마다가 아니라 화면 묶음마다** 같은 값.

**문장의 세로 범위는 글자 줄만으로 재면 안 된다.** `.inputBox` 는 `inline-flex` 라
줄 상자보다 위아래로 최대 10px 삐져나온다(밑줄 포함).
`min(줄 top, 자식 .inputBox top)` ~ `max(줄 bot, 자식 .inputBox bot)` 으로 잡고
위아래 **여백 20px**(`SAFE`)을 더 둔다.

1. **묶음 나누기**
   - `.Paragraph` 가 바뀌면 무조건 새 묶음 (번호 대화 경계)
   - 4개 탭을 모두 렌더해서 **어느 탭에서든** 문장 bottom 이
     `묶음 첫 문장 top + (548 − 여백)` 을 넘으면 새 묶음
     (여백: 첫 묶음 20px, 나머지 40px)
   - **넘침으로 끊을 때 한 화자 발화(`comp-script-box`) 중간에서 끊으면 안 된다.**
     끊을 자리를 그 발화의 첫 문장까지 앞당긴다. 발화 하나가 한 화면보다 큰 경우만 예외.
   - `comp-script-box` 경계는 **강제하지 않는다** — L6 Reading 4 의 팁 목록처럼
     짧은 박스가 여러 개면 줄마다 스크롤이 튄다.
2. **값 (탭마다 따로)** — 첫 묶음은 속성을 넣지 않는다.
   ```
   lo = 묶음 마지막 문장 bottom + 20 − 548
   hi = 묶음 첫 문장 top − 20
   scrollTop = clamp(첫 문장 top − 20, lo, hi) → 다시 [0, maxScroll] 로 클램프
   ```
3. **`numberScrollTop`** = 그 box 첫 문장의 `scrollTop`

문서 끝 묶음은 maxScroll 에 걸려 첫 문장이 화면 위까지 못 올라올 수 있다 — 잘림은 아니다.

**순서 주의**: 화자 칸 폭을 확정하기 전에 스크롤을 재면 안 된다. 폭이 바뀌면 줄바꿈이 달라진다.
`build3.py` 는 (1) 정답·화자 폭 측정 → (2) 그 폭 적용해 다시 렌더 → (3) 묶음·scrollTop 계산, 2단계.

---

## 7. 검증

```bash
python3 verify.py measure/out
```
- `‖` · 빈 `itemIdx` : 0
- `itemIdx` 연속성(0,1,2…) 과 빈칸 개수 일치
- 문장 잘림 0
- 입력칸 폭부족 0
- 발화 중간 끊김 — 독백·긴 문단만 허용

**본문 대조** (원고와 글자가 같은지)
```python
import re, os, difflib, src, dlg   # rd 면 rd
k=lambda s: re.sub(r'[^a-z0-9]','',s.lower().replace('’',"'"))
for p in dlg.LESSONS[N]:
    h=open('measure/out/%s_dic1.html'%p['page'],encoding='utf-8').read()
    for ti,t in enumerate(re.split(r'js-quizContainer|attachSticker',h)[1:]):
        got=[]
        for m in re.finditer(r'<script-cont\b([^>]*)>',t):
            a=m.group(1); e=re.search(r'\ben="([^"]*)"',a); s=re.search(r'\bsticker="([^"]*)"',a)
            if e: got.append(re.sub(r'\|\|','',e.group(1)))
            elif s: got.append(s.group(1).replace('|',''))
        got=src.norm(' '.join(got)).replace('&quot;','"').replace('&amp;','&')
        lv=('보충','기본','심화')[ti] if ti<3 else '보충'
        want=src.norm(' '.join(x[2] for x in dlg.dict_lines(N,p['sec'],lv)))
        want=src.norm(src.strip_marks(want))
        r=difflib.SequenceMatcher(None,k(got),k(want)).ratio()
        if r<0.999: print(p['page'],ti+1,r)
```

**구조 확인**: CRLF · 끝 개행 없음 · `<div>` 짝 맞음
```bash
python3 -c "
import sys,glob
for f in sorted(glob.glob('measure/out/*.html')):
    d=open(f,'rb').read()
    print(f.split('/')[-1], len(d), 'CRLF' if d.count(b'\r\n')==d.count(b'\n') else 'MIXED',
          'div',d.count(b'<div'),d.count(b'</div>'), '끝개행',d.endswith(b'\n'))"
```

---

## 8. 그동안 겪은 함정 (같은 실수 반복 금지)

1. **14px 보정** — 6장 참고. 제일 크게 데인 부분.
2. **`‖` 누출** — 정답 묶음 한가운데를 자르면 `‖` 가 그대로 나가고 `itemIdx=""` 가 되어
   재생·하이라이트가 깨진다. 생성기에 `assert` 로 막아뒀다.
3. **발화 중간 스크롤 끊김** — 같은 말풍선 안에서 앞 문장은 화면 아래, 다음 문장은 위로 튄다.
4. **`numberScrollTop` 0 고정** — 번호 배지가 안 따라간다.
5. **input 칸 삐져나옴** — 문장 높이를 글자 줄만으로 재면 안 된다.
6. **화자 칸 폭 확정 전 스크롤 측정** — 순서 지킬 것.
7. **음원 파일명 임의 수정 금지** — 빨간 글씨는 추가 녹음분, 아직 없을 뿐이다.
8. **파일명 뒤 번호 추정 금지** — 단원마다 다르다. 사용자에게 확인.
9. **3단원 원본 수정 금지** — 참고용. 렌더링 때 임시 파일을 그 폴더에 쓰면 반드시 지울 것.
10. **원고 내용을 임의로 바꾸지 말 것.** 이상해 보이면 보고만 한다.
    (예: L8 미니 단어장 2행 `come true` 의 뜻·예문이 `adventure` 것으로 들어가 있음)

---

## 9. 스크립트

| 파일 | 역할 |
|---|---|
| `src.py` | 엑셀 경로·공통 파싱(빨강 런 → `\|\|빈칸\|\|`, itemIdx, 정규화) |
| `gen.py` | Reading형 HTML 템플릿(HEAD/QUIZ_TAB/TAB4/FOOT) · sticker 생성 |
| `dlg.py` | **대화형 생성기** — 화자·번호 대화·인터뷰. `cut_words()` 정렬 |
| `rd.py` | **Reading 산문 생성기** — 소제목(`subTitBox`) 지원 |
| `measure/opt.py` | 렌더 유틸(`parse`/`inject`/`render`) — build3 가 `exec` 로 가져온다 |
| `measure/geo.js` | 브라우저 측정 JS (문장 top/bot, 문단·박스 인덱스, 화자 유무) |
| `measure/build3.py` | **입력칸 폭 + scrollTop 주입** (14px 보정 포함) |
| `verify.py` | 산출물 일괄 검증 |

폴더 규칙
```
/home/claude/cj/measure/work/popup/   생성기가 만든 본문 (폭·scrollTop 없음)
/home/claude/cj/measure/out/          build3 가 완성한 최종본
/home/claude/cj/measure/reading/      L8 Reading 원본 백업(다시 돌릴 때 work 로 복사)
```

---

## 10. 남은 이슈 / 미착수

- **Reading 본문 페이지**(`pNNN_NN.html` + `_all`/`_kor1` 팝업 + css/js)는 아직 못 만든다.
  필요한 것: ① `images/pNNN_NN/` 배경·타이틀 이미지(신규 단원엔 없음)
  ② 한글 해석(두 엑셀에 없음, 지도서 PDF 확인 필요) ③ `kr` 안의 레이아웃 수치
- L7 `3_118_lnt_a_15_1.mp3` 미납품 (대본 빨간 글씨). 파일 들어오면 해결.
- L8 미니 단어장 2행 `come true` 뜻·예문 오류 — 편집자 확인 필요.
- 1~5단원은 미착수(앞부분 팝업은 이미 있음, Reading 은 없음).
